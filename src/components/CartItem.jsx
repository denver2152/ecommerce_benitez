import PropTypes from 'prop-types'
import { Link } from 'react-router-dom'
import { formatPrice } from '../utils/formatPrice'
function CartItem({ item, onUpdateQuantity, onRemove }) { return <article className="cart-item"><img src={item.image} alt={item.name} /><div><Link to={`/product/${item.id}`}><h3>{item.name}</h3></Link><p className="unit-price">{formatPrice(item.price)} each</p></div><div className="cart-quantity"><button className="icon-button" type="button" aria-label={`Decrease ${item.name} quantity`} onClick={() => onUpdateQuantity(item.id, item.quantity - 1)} disabled={item.quantity === 1}>-</button><span className="quantity-value">{item.quantity}</span><button className="icon-button" type="button" aria-label={`Increase ${item.name} quantity`} onClick={() => onUpdateQuantity(item.id, item.quantity + 1)} disabled={item.quantity === item.stock}>+</button></div><div className="cart-subtotal">{formatPrice(item.price * item.quantity)}<button className="remove-button" type="button" onClick={() => onRemove(item.id)}>Remove</button></div></article> }
CartItem.propTypes = { item: PropTypes.shape({ id: PropTypes.number.isRequired, name: PropTypes.string.isRequired, price: PropTypes.number.isRequired, image: PropTypes.string.isRequired, quantity: PropTypes.number.isRequired, stock: PropTypes.number.isRequired }).isRequired, onUpdateQuantity: PropTypes.func.isRequired, onRemove: PropTypes.func.isRequired }
export default CartItem
